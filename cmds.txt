ssh cuda5

cd ../../scratch/mc10322

git clone git@github.com:Monishver11/modded-nanogpt-moe.git

cd modded-nanogpt-moe

conda create -n nanogpt python=3.10 -y

conda activate nanogpt

pip install -r requirements.txt \
    --extra-index-url https://download.pytorch.org/whl/cu126

pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cu126 --upgrade

python3 - <<EOF
import torch
print(torch.__version__, torch.version.cuda)
EOF

2.9.1+cu128 12.8

# downloads only the first 900M training tokens to save time
python data/cached_fineweb10B.py 9

python train_gpt.py

watch -n 1 nvidia-smi
nvitop

# Clone and install ScatterMoE (it needs to compile the Triton kernels)

git clone https://github.com/shawntan/scattermoe
cd scattermoe
pip install -e .

TORCH_LOGS="graph_breaks" python train_gpt_scatter_moe.py

# Install MegaBlocks
cd ..
git clone https://github.com/stanford-futuredata/megablocks.git
cd megablocks
pip install -e .

# 1. Uninstall broken packages
pip uninstall -y stanford-stk megablocks

# 2. Re-install stanford-stk forcing usage of scratch for compilation
# (This compiles the CUDA kernels, which takes time and space)
TMPDIR=/scratch/mc10322/tmp pip install stanford-stk --no-cache-dir --force-reinstall

# 3. Re-install megablocks
pip install megablocks

# Verify
python -c "import stk; print('STK Version:', stk.__version__)"
python -c "import megablocks.ops; print(True)"

pip install git+https://github.com/tgale96/grouped_gemm@main
python -c "import grouped_gemm; print('Grouped GEMM installed successfully!')"


# Clear pip cache (often gigabytes)
rm -rf ~/.cache/pip
# Clear conda cache (if using conda)
conda clean --all -y
quota -s

# git cmds;
git pull --rebase origin scatter-moe-package
git push

cd ..
git clone git@github.com:Monishver11/deepseek_moe_fused_repo.git
cd deepseek_moe_fused_repo/
pip install -e .

python -c "import deepseek_moe_fused; print('works')"

pip uninstall -y deepseek_moe_fused



# NYU A100 CPU Only Work

ssh-keygen -R greene.hpc.nyu.edu 

ssh mc10322@greene.hpc.nyu.edu

ssh burst

srun --account=csci_ga_3033-2025fa --partition=interactive --pty /bin/bash

nvidia-smi

cd /scratch/mc10322

scp greene-dtn:/scratch/work/public/overlay-fs-ext3/overlay-25GB-500K.ext3.gz .

gunzip -vvv ./overlay-25GB-500K.ext3.gz

scp -rp greene-dtn:/scratch/work/public/singularity/cuda12.8.1-cudnn9.8.0-ubuntu24.04.2.sif .

singularity exec --bind /scratch --nv --overlay  /scratch/mc10322/overlay-25GB-500K.ext3:rw /scratch/mc10322/cuda12.8.1-cudnn9.8.0-ubuntu24.04.2.sif /bin/bash

cd /ext3/
wget --no-check-certificate https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh
sh Miniforge3-Linux-x86_64.sh -b -p /ext3/miniforge3

touch /ext3/env.sh
echo '#!/bin/bash' >> /ext3/env.sh
echo 'unset -f which' >> /ext3/env.sh
echo 'source /ext3/miniforge3/etc/profile.d/conda.sh' >> /ext3/env.sh
echo 'export PATH=/ext3/miniforge3/bin:$PATH'         >> /ext3/env.sh
echo 'export PYTHONPATH=/ext3/miniforge3/bin:$PATH'   >> /ext3/env.sh

source /ext3/env.sh

conda config --remove channels defaults
conda update -n base conda -y
conda clean --all --yes

# NYU A100 GPU Only Work;

srun --account=csci_ga_3033-2025fa --partition=c12m85-a100-1 --gres=gpu:1 --time=04:00:00 --pty /bin/bash

cd /scratch/mc10322

singularity exec \
  --bind /scratch \
  --nv \
  --overlay /scratch/mc10322/overlay-25GB-500K.ext3:rw \
  /scratch/mc10322/cuda12.8.1-cudnn9.8.0-ubuntu24.04.2.sif \
  /bin/bash

source /ext3/env.sh
conda activate nanogpt

